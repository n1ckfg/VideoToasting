1. Switch to your /workspace folder.
2. Read video_toaster_methods.md which is a list of effects used by the 1990s video effects system called the Video Toaster.
3. Make an index.html listing all effects with a brief summary from video_toaster_methods.md and mark each of them in gray as "unimplemented"
4. Create a named html page for each effect and link it in index.html
5. Read the first effects html page, load the p5.js library, and write a glsl shader implementing the effect based on its description in video_toaster_methods.md
6. Using your headless browser, attempt to load the page. If it has errors, rewrite the code and try again.  Repeat this 20 times before timing out.
7. If you are able to load the page without errors before the timeout, mark it in index.html in green as "success" and move on to the next entry. If you are not able to resolve all the errors within the timeout, mark it in red as "failure" in index.html and move on to the next.
8. Once you have evaluated all effects in the list as success or failure, post "Final tally: " with the number of successes and failures at the bottom of index.html, and a timestamp.

